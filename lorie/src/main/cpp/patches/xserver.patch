diff --git a/Xext/shm.c b/Xext/shm.c
index 1eed91bc552e249e2fb1141d9082ef088c67a235..7405223e759d6d4eef9807714d58b31c34843c5b 100644
--- a/Xext/shm.c
+++ b/Xext/shm.c
@@ -195,0 +196,4 @@ CheckForShmSyscall(void)
+const DevPrivateKey ShmGetDevPrivateKeyRec(void) {
+    return shmPixmapPrivateKey;
+}
+
diff --git a/Xext/shmint.h b/Xext/shmint.h
index 9dadea756db60d4160145da3db03a259867a68dd..e4e6f4db6fa8f03530287c97a3cc2efda2d6975d 100644
--- a/Xext/shmint.h
+++ b/Xext/shmint.h
@@ -82,0 +83,2 @@ typedef struct _ShmDesc {
+extern const DevPrivateKey ShmGetDevPrivateKeyRec(void);
+
diff --git a/dix/dixutils.c b/dix/dixutils.c
index 331ccfb963eb7fdf9a5350ae1fc608b4d1cd4875..f9e7fe357c2480fc32bbedcbc95f4f22f1161dfa 100644
--- a/dix/dixutils.c
+++ b/dix/dixutils.c
@@ -506,0 +507,5 @@ InitBlockAndWakeupHandlers(void)
+#define ClearWorkQueue _ClearWorkQueue
+#define ProcessWorkQueue _ProcessWorkQueue
+#define ProcessWorkQueueZombies _ProcessWorkQueueZombies
+#define QueueWorkProc _QueueWorkProc
+
@@ -585,0 +591,5 @@ QueueWorkProc(Bool (*function) (ClientPtr pClient, void *closure),
+#undef ClearWorkQueue
+#undef ProcessWorkQueue
+#undef ProcessWorkQueueZombies
+#undef QueueWorkProc
+
diff --git a/dix/enterleave.c b/dix/enterleave.c
index 18b5d66cd3c84c611fba44f9c83391db37f118e6..a6fa80d8bae894a3ddb0f92b2660931641104135 100644
--- a/dix/enterleave.c
+++ b/dix/enterleave.c
@@ -1543,0 +1544,2 @@ DeviceFocusEvents(DeviceIntPtr dev, WindowPtr from, WindowPtr to, int mode)
+extern void DDXNotifyFocusChanged(void);
+
@@ -1553,0 +1556,3 @@ DoFocusEvents(DeviceIntPtr pDev, WindowPtr from, WindowPtr to, int mode)
+    if (from != to)
+        DDXNotifyFocusChanged();
+
diff --git a/dix/main.c b/dix/main.c
index 38e29cebbe7c82c195e11f8d47805b44d459c780..e4957e9bc30601a4bc43936aea08779f9cc5a98c 100644
--- a/dix/main.c
+++ b/dix/main.c
@@ -270,0 +271 @@ dix_main(int argc, char *argv[], char *envp[])
+        ddxReady();
diff --git a/include/os.h b/include/os.h
index 7db24088f8512f2eb149d47eb807d5d6a33a0e19..b782360bcbf2969fc3f9d32aa1acb1d1eff3abab 100644
--- a/include/os.h
+++ b/include/os.h
@@ -559,0 +560,2 @@ extern _X_EXPORT void
+ddxReady(void);
+extern _X_EXPORT void
diff --git a/include/xkbfile.h b/include/xkbfile.h
index f93d31ab070333c9a03947ba67346dfefce2cff8..f65338ac4628874ddf427cddc9dcc74f77e2052a 100644
--- a/include/xkbfile.h
+++ b/include/xkbfile.h
@@ -81,0 +82,30 @@ typedef void (*XkbFileAddOnFunc) (FILE * /* file */ ,
+#define XkbInternAtom XXkbInternAtom
+#define XkbAtomText XXkbAtomText
+#define XkbStringText XXkbStringText
+#define XkbVModIndexText XXkbVModIndexText
+#define XkbVModMaskText XXkbVModMaskText
+#define XkbModMaskText XXkbModMaskText
+#define XkbModIndexText XXkbModIndexText
+#define XkbConfigText XXkbConfigText
+#define XkbKeysymText XXkbKeysymText
+#define XkbKeyNameText XXkbKeyNameText
+#define XkbSIMatchText XXkbSIMatchText
+#define XkbIMWhichStateMaskText XXkbIMWhichStateMaskText
+#define XkbControlsMaskText XXkbControlsMaskText
+#define XkbGeomFPText XXkbGeomFPText
+#define XkbDoodadTypeText XXkbDoodadTypeText
+#define XkbActionTypeText XXkbActionTypeText
+#define XkbActionText XXkbActionText
+#define XkbBehaviorText XXkbBehaviorText
+#define XkbIndentText XXkbIndentText
+#define XkbWriteXKBKeycodes XXkbWriteXKBKeycodes
+#define XkbWriteXKBKeyTypes XXkbWriteXKBKeyTypes
+#define XkbWriteXKBCompatMap XXkbWriteXKBCompatMap
+#define XkbWriteXKBSymbols XXkbWriteXKBSymbols
+#define XkbWriteXKBGeometry XXkbWriteXKBGeometry
+#define XkbWriteXKBKeymapForNames XXkbWriteXKBKeymapForNames
+#define XkbFindKeycodeByName XXkbFindKeycodeByName
+#define XkbConvertGetByNameComponents XXkbConvertGetByNameComponents
+#define XkbNameMatchesPattern XXkbNameMatchesPattern
+#define _XkbKSCheckCase _XXkbKSCheckCase
+
diff --git a/os/connection.c b/os/connection.c
index 35e2a4d45a438596d8dab361bb4a73e56bb2b487..2e80d88353326877c3b1d2a90563624fca5320e1 100644
--- a/os/connection.c
+++ b/os/connection.c
@@ -187,0 +188 @@ InitParentProcess(void)
+    const char *notifyParent = getenv("TERMUX_X11_NOTIFY_PARENT");
@@ -190 +191 @@ InitParentProcess(void)
-    if (handler == SIG_IGN)
+    if (handler == SIG_IGN || (notifyParent && !strcmp(notifyParent, "1")))
diff --git a/os/osinit.c b/os/osinit.c
index 8575319fff617e2f56cc6d14078312f07c8e3356..a6d0dfcccd363640dbf17a119e31ba9c0d7781c3 100644
--- a/os/osinit.c
+++ b/os/osinit.c
@@ -173 +173 @@ OsInit(void)
-#if !defined(WIN32) || defined(__CYGWIN__)
+#if !defined(WIN32) || defined(__CYGWIN__) || !defined(__ANDROID__)
@@ -177,3 +177 @@ OsInit(void)
-        int siglist[] = { SIGSEGV, SIGQUIT, SIGILL, SIGFPE, SIGBUS,
-            SIGABRT,
-            SIGSYS,
+        int siglist[] = { SIGQUIT, SIGILL, SIGFPE,
diff --git a/os/utils.c b/os/utils.c
index 008ed0463930370aa673c57aa1a7fdc52d471309..fae554b22e49b801bd278da4ed835c16b6f32110 100644
--- a/os/utils.c
+++ b/os/utils.c
@@ -216,0 +217,2 @@ OsSignal(int sig, OsSigHandlerPtr handler)
+#elif defined(__ANDROID__)
+    return SIG_DFL;
@@ -237 +239 @@ OsSignal(int sig, OsSigHandlerPtr handler)
-#define LOCK_DIR "/tmp"
+#define LOCK_DIR (getenv("TMPDIR") ?: "/tmp")
@@ -329 +331 @@ LockServer(void)
-        haslock = (link(tmp, LockFile) == 0);
+        haslock = (rename(tmp, LockFile) == 0);
@@ -1423,0 +1426,33 @@ static struct pid {
+#include <fcntl.h>
+#include <android/log.h>
+
+extern char* xkbcomp_argv[];
+extern int xkbcomp_argc;
+int xkbcomp_main(int argc, char *argv[]);
+
+static int xkbcomp_output_fd = -1;
+
+/* xkbcomp complains about every keysym missing from the xorgproto it was built with, and
+ * the client never sees those keys anyway, so only a failed keymap reaches the terminal. */
+static void
+ReportXkbcompOutput(Bool failed)
+{
+    static char output[65536];
+    int cap = sizeof(output) - 1, len = 0, r;
+
+    if (xkbcomp_output_fd < 0)
+        return;
+
+    while (len < cap && (r = read(xkbcomp_output_fd, output + len, cap - len)) > 0)
+        len += r;
+    output[len] = 0;
+    close(xkbcomp_output_fd);
+    xkbcomp_output_fd = -1;
+
+    if (failed)
+        fputs(output, stderr);
+    else
+        for (char *line = strtok(output, "\n"); line; line = strtok(NULL, "\n"))
+            __android_log_write(ANDROID_LOG_DEBUG, "xkbcomp", line);
+}
+
@@ -1429 +1464 @@ Popen(const char *command, const char *type)
-    int pdes[2], pid;
+    int pdes[2], odes[2] = { -1, -1 }, pid;
@@ -1444,0 +1480,4 @@ Popen(const char *command, const char *type)
+    if (pipe(odes) == 0)
+        /* Nothing drains this one until xkbcomp is done, so a full pipe must not block it. */
+        fcntl(odes[1], F_SETFL, O_NONBLOCK);
+
@@ -1459,0 +1499,2 @@ Popen(const char *command, const char *type)
+        close(odes[0]);
+        close(odes[1]);
@@ -1487,2 +1528,12 @@ Popen(const char *command, const char *type)
-        execl("/bin/sh", "sh", "-c", command, (char *) NULL);
-        _exit(127);
+
+        if (odes[1] >= 0) {
+            close(odes[0]);
+            dup2(odes[1], 1);
+            dup2(odes[1], 2);
+            close(odes[1]);
+        }
+
+        for(int j=1; j<= SIGUNUSED; j++)
+            signal(j, SIG_DFL);
+
+        _exit(xkbcomp_main(xkbcomp_argc, xkbcomp_argv));
@@ -1494,0 +1546,5 @@ Popen(const char *command, const char *type)
+    if (odes[1] >= 0) {
+        close(odes[1]);
+        xkbcomp_output_fd = odes[0];
+    }
+
@@ -1630,0 +1687,2 @@ Pclose(void *iop)
+    ReportXkbcompOutput(pid == -1 || !WIFEXITED(pstat) || WEXITSTATUS(pstat) != 0);
+
diff --git a/present/present.c b/present/present.c
index 271fe32bc8b389498171dc572f17f5cd611ff167..560e24b1614ec9bf0dd9c49a87f05069f9b4a07d 100644
--- a/present/present.c
+++ b/present/present.c
@@ -238 +238,2 @@ present_pixmap(WindowPtr window,
-               int num_notifies)
+               int num_notifies,
+               const present_lorie_feedback_target_rec *lorie_feedback)
@@ -258 +259,2 @@ present_pixmap(WindowPtr window,
-                                       num_notifies);
+                                       num_notifies,
+                                       lorie_feedback);
@@ -276 +278 @@ present_notify_msc(WindowPtr window,
-                          target_msc, divisor, remainder, NULL, 0);
+                          target_msc, divisor, remainder, NULL, 0, NULL);
diff --git a/present/present_event.c b/present/present_event.c
index 849732dc880c4627ff80ae8eed6d3794d906e2d6..29b04f2f293ae68afba14c3c0733b5418b5f2f3e 100644
--- a/present/present_event.c
+++ b/present/present_event.c
@@ -25,0 +26 @@ static RESTYPE present_event_type;
+static uint64_t present_lorie_event_generation;
@@ -178,0 +180,69 @@ present_send_complete_notify(WindowPtr window, CARD8 kind, CARD8 mode, CARD32 se
+/* Private, capability-negotiated delivery to the exact event selection which
+ * opted in.  Standard CompleteNotify remains broadcast according to the
+ * Present protocol; this additional kind must never reach legacy clients. */
+Bool
+present_lorie_get_feedback_target(WindowPtr window, ClientPtr client,
+                                  present_lorie_feedback_target_rec *target)
+{
+    present_window_priv_ptr window_priv = present_window_priv(window);
+    present_event_ptr event;
+
+    if (!window_priv || !target)
+        return FALSE;
+
+    for (event = window_priv->events; event; event = event->next) {
+        if (event->client == client &&
+            (event->mask & PresentCompleteNotifyMask)) {
+            *target = (present_lorie_feedback_target_rec) {
+                .eid = event->id,
+                .window_generation = window_priv->lorie_generation,
+                .event_generation = event->lorie_generation,
+            };
+            return TRUE;
+        }
+    }
+
+    return FALSE;
+}
+
+Bool
+present_lorie_send_actual_notify(WindowPtr window,
+                                 const present_lorie_feedback_target_rec *target,
+                                 CARD8 mode, CARD32 serial, uint64_t ust,
+                                 uint64_t tag)
+{
+    present_window_priv_ptr window_priv = present_window_priv(window);
+    present_event_ptr event;
+
+    if (!window_priv || !target || !target->eid ||
+        window_priv->lorie_generation != target->window_generation)
+        return FALSE;
+
+    for (event = window_priv->events; event; event = event->next) {
+        xPresentCompleteNotify cn;
+
+        if (event->id != target->eid ||
+            event->lorie_generation != target->event_generation ||
+            !(event->mask & PresentCompleteNotifyMask))
+            continue;
+
+        cn = (xPresentCompleteNotify) {
+            .type = GenericEvent,
+            .extension = present_request,
+            .length = (sizeof(xPresentCompleteNotify) - 32) >> 2,
+            .evtype = PresentCompleteNotify,
+            .kind = LoriePresentCompleteKindActual,
+            .mode = mode,
+            .eid = event->id,
+            .window = window->drawable.id,
+            .serial = serial,
+            .ust = ust,
+            .msc = tag,
+        };
+        WriteEventsToClient(event->client, 1, (xEvent *) &cn);
+        return TRUE;
+    }
+
+    return FALSE;
+}
+
@@ -249,0 +320,3 @@ present_select_input(ClientPtr client, XID eid, WindowPtr window, CARD32 mask)
+    event->lorie_generation = ++present_lorie_event_generation;
+    if (!event->lorie_generation)
+        event->lorie_generation = ++present_lorie_event_generation;
diff --git a/present/present_execute.c b/present/present_execute.c
index 68a5878be9c3f9698e4905c36965cf1e5b6efc89..b97dadf7f749c5714a89610b4a44818159f18678 100644
--- a/present/present_execute.c
+++ b/present/present_execute.c
@@ -35,0 +36,2 @@ present_wait_fence_triggered(void *param)
+    /* lorie: disarm, or miSyncTriggerFence()'s restart-scan re-fires this callback. */
+    present_fence_set_callback(vblank->wait_fence, NULL, NULL);
@@ -69,0 +72,8 @@ present_execute_copy(present_vblank_ptr vblank, uint64_t crtc_msc)
+    /* lorie: screen_x/screen_y is dst's own (0,0) in screen coords; window.xy minus that gives the
+     * window's offset within dst, whether dst is root, itself, or an inherited redirected ancestor. */
+    PixmapPtr                   gpuCopyDst = screen->GetWindowPixmap(window);
+    int16_t                     gpuCopyXOff = (int16_t) (vblank->x_off + window->drawable.x - gpuCopyDst->screen_x);
+    int16_t                     gpuCopyYOff = (int16_t) (vblank->y_off + window->drawable.y - gpuCopyDst->screen_y);
+    /* lorie: renderer can be connected but still unable to ever finish a pending GPU copy
+     * (e.g. activity backgrounded, no surface) - completedSerial won't advance either way. */
+    Bool                         gpuCopyStalled = !lorieConnectionAlive() || !lorieRendererAvailable();
@@ -82 +92,70 @@ present_execute_copy(present_vblank_ptr vblank, uint64_t crtc_msc)
-    present_copy_region(&window->drawable, vblank->pixmap, vblank->update, vblank->x_off, vblank->y_off);
+    /* lorie: a GPU copy was already scheduled; poll for completion instead of blocking. */
+    if (vblank->gpu_copy_pending && !lorieGpuCopyIsDone(vblank->gpu_copy_serial) &&
+        !gpuCopyStalled &&
+        Success == screen_priv->queue_vblank(screen, window, vblank->crtc, vblank->event_id, crtc_msc + 1)) {
+        vblank->queued = TRUE;
+        return;
+    }
+
+    if (!vblank->lorie_present_tag)
+        vblank->lorie_present_tag = loriePreparePresentTag(window, gpuCopyDst);
+
+    if (vblank->gpu_copy_pending) {
+        lorieGpuCopyAck(vblank->pixmap, vblank->gpu_copy_dst_buffer);
+        vblank->gpu_copy_pending = FALSE;
+        if (gpuCopyStalled && !lorieGpuCopyIsDone(vblank->gpu_copy_serial)) {
+            /* lorie: the renderer never actually finished this copy - tell the client it was
+             * skipped instead of falsely reporting it as presented. */
+            vblank->lorie_feedback_handed_off = FALSE;
+            present_vblank_scrap(vblank);
+            screen_priv->flush(window);
+            return;
+        }
+    } else if (lorieTryScheduleGpuCopy(vblank->pixmap, gpuCopyDst, vblank->update, gpuCopyXOff, gpuCopyYOff,
+                                        vblank->lorie_present_tag, window->drawable.id, vblank->serial,
+                                        vblank->lorie_feedback.eid,
+                                        vblank->lorie_feedback.window_generation,
+                                        vblank->lorie_feedback.event_generation,
+                                        &vblank->gpu_copy_serial, &vblank->gpu_copy_dst_buffer)) {
+        if (vblank->lorie_present_tag)
+            vblank->lorie_feedback_handed_off = TRUE;
+        /* lorie: our GPU blit writes the destination pixmap directly, bypassing the normal GC
+         * ops that Damage tracking hooks into, so report it manually - same drawable and region
+         * present_scmd.c's flip success path already uses, so this also reaches Composite's
+         * per-window damage for redirected windows, not just lorie's own root damage. */
+        RegionPtr damage = vblank->update ? vblank->update : &window->clipList;
+        if (vblank->update)
+            RegionIntersect(damage, damage, &window->clipList);
+        DamageDamageRegion(&window->drawable, damage);
+
+        /* lorie: copy offloaded to the renderer's GPU context. The region was already
+         * consumed (copied into the shared command queue), so free it like
+         * present_copy_region would have. */
+        if (vblank->update) {
+            RegionDestroy(vblank->update);
+            vblank->update = NULL;
+        }
+        if (Success == screen_priv->queue_vblank(screen, window, vblank->crtc, vblank->event_id, crtc_msc + 1)) {
+            vblank->gpu_copy_pending = TRUE;
+            vblank->queued = TRUE;
+            return;
+        }
+        /* Failed to requeue for polling (e.g. OOM) - release our extra ref and treat the
+         * presumably still in-flight GPU copy as done; same best-effort fallback as above. */
+        lorieGpuCopyAck(vblank->pixmap, vblank->gpu_copy_dst_buffer);
+    } else {
+        present_copy_region(&window->drawable, vblank->pixmap, vblank->update, vblank->x_off, vblank->y_off);
+
+        loriePublishPresentTag(vblank->lorie_present_tag,
+                               window->drawable.id, vblank->serial,
+                               vblank->lorie_feedback.eid,
+                               vblank->lorie_feedback.window_generation,
+                               vblank->lorie_feedback.event_generation);
+        if (vblank->lorie_present_tag)
+            vblank->lorie_feedback_handed_off = TRUE;
+
+        /* present_copy_region sticks the region into a scratch GC,
+         * which is then freed, freeing the region
+         */
+        vblank->update = NULL;
+    }
@@ -84,4 +162,0 @@ present_execute_copy(present_vblank_ptr vblank, uint64_t crtc_msc)
-    /* present_copy_region sticks the region into a scratch GC,
-     * which is then freed, freeing the region
-     */
-    vblank->update = NULL;
diff --git a/present/present_priv.h b/present/present_priv.h
index 4ad729864759ce67668545299f355337540386eb..778ebeb189d396d2690a60f4d182ce2ec3902c40 100644
--- a/present/present_priv.h
+++ b/present/present_priv.h
@@ -26,0 +27 @@
+#include <stdbool.h>
@@ -50,0 +52,7 @@
+/* Private Termux:X11 protocol negotiated through QueryCapabilities. */
+#define LoriePresentCapabilityActualFeedback (1U << 28)
+#define LoriePresentOptionActualFeedback     (1U << 31)
+#define LoriePresentCompleteKindActual       2
+#define LoriePresentCompleteModeActual       1
+#define LoriePresentCompleteModeUnknown      2
+
@@ -58,0 +67,6 @@ typedef struct present_notify present_notify_rec, *present_notify_ptr;
+typedef struct {
+    XID eid;
+    uint64_t window_generation;
+    uint64_t event_generation;
+} present_lorie_feedback_target_rec;
+
@@ -92,0 +107,10 @@ struct present_vblank {
+
+    /* lorie: set when present_execute_copy offloaded the copy to the renderer's GPU context;
+     * the vblank stays queued until the copy is acknowledged done. */
+    Bool                gpu_copy_pending;
+    uint64_t            gpu_copy_serial;
+    void                *gpu_copy_dst_buffer; /* opaque LorieBuffer*, held for lorieGpuCopyAck */
+    uint64_t            lorie_present_tag; /* DEBUG: root-visible Present correlation identity */
+    present_lorie_feedback_target_rec lorie_feedback;
+    Bool                lorie_feedback_handed_off;
+    Bool                lorie_feedback_terminal_sent;
@@ -94,0 +119,27 @@ struct present_vblank {
+/* lorie: hooks implemented in lorie/InitOutput.c, used by present_execute_copy() to offload the
+ * pixmap->dst copy (dst being whatever GetWindowPixmap(window) is - root, or a Composite-redirected
+ * window's own backing pixmap) to the renderer's GPU context instead of a CPU CopyArea. */
+extern Bool lorieTryScheduleGpuCopy(PixmapPtr pixmap, PixmapPtr dst, RegionPtr update, int16_t x_off, int16_t y_off,
+                                     uint64_t present_tag, uint32_t present_window, uint32_t present_serial,
+                                     uint32_t feedback_eid, uint64_t window_generation,
+                                     uint64_t event_generation,
+                                     uint64_t *out_serial, void **out_dst_buffer);
+extern Bool lorieGpuCopyIsDone(uint64_t serial);
+extern void lorieGpuCopyAck(PixmapPtr pixmap, void *dst_buffer);
+/* DEBUG: application Present-to-Android-frame correlation.  These hooks only
+ * tag direct screen-pixmap content; they never signal readiness or ownership. */
+extern uint64_t loriePreparePresentTag(WindowPtr window, PixmapPtr dst);
+extern void loriePublishPresentTag(uint64_t tag, uint32_t window, uint32_t serial,
+                                   uint32_t feedback_eid,
+                                   uint64_t window_generation,
+                                   uint64_t event_generation);
+/* lorie: implemented in lorie/cmdentrypoint.c. Avoids waiting forever on a GPU copy that a dead renderer will never finish. */
+extern bool lorieConnectionAlive(void);
+/* lorie: implemented in lorie/InitOutput.c. Renderer can be connected but still unable to
+ * draw (e.g. activity backgrounded, no surface) - same "give up on this GPU copy" cases as
+ * lorieConnectionAlive() need to check this too. */
+extern bool lorieRendererAvailable(void);
+
+/* lorie: implemented in present_scmd.c; re-executes any vblank whose GPU copy has finished. */
+extern void lorieRecheckGpuCopies(void);
+
@@ -132 +183,2 @@ typedef int (*present_priv_pixmap_ptr)(WindowPtr window,
-                                       int num_notifies);
+                                       int num_notifies,
+                                       const present_lorie_feedback_target_rec *lorie_feedback);
@@ -214,0 +267 @@ typedef struct present_event {
+    uint64_t lorie_generation;
@@ -224,0 +278 @@ struct present_window_priv {
+    uint64_t               lorie_generation;
@@ -298 +352,2 @@ present_pixmap(WindowPtr window,
-               int num_notifies);
+               int num_notifies,
+               const present_lorie_feedback_target_rec *lorie_feedback);
@@ -319,0 +375,10 @@ present_send_complete_notify(WindowPtr window, CARD8 kind, CARD8 mode, CARD32 se
+Bool
+present_lorie_get_feedback_target(WindowPtr window, ClientPtr client,
+                                  present_lorie_feedback_target_rec *target);
+
+Bool
+present_lorie_send_actual_notify(WindowPtr window,
+                                 const present_lorie_feedback_target_rec *target,
+                                 CARD8 mode, CARD32 serial, uint64_t ust,
+                                 uint64_t tag);
+
@@ -472 +537,2 @@ present_vblank_init(present_vblank_ptr vblank,
-                    uint64_t crtc_msc);
+                    uint64_t crtc_msc,
+                    const present_lorie_feedback_target_rec *lorie_feedback);
@@ -490 +556,2 @@ present_vblank_create(WindowPtr window,
-                      uint64_t crtc_msc);
+                      uint64_t crtc_msc,
+                      const present_lorie_feedback_target_rec *lorie_feedback);
diff --git a/present/present_request.c b/present/present_request.c
index f3e5679b5d1c182d2a0e89b2a27506a000e7fdb7..418e30cff66baeab03834d6497ce4f95f229e20b 100644
--- a/present/present_request.c
+++ b/present/present_request.c
@@ -94,0 +95,2 @@ proc_present_pixmap(ClientPtr client)
+    present_lorie_feedback_target_rec lorie_feedback = { 0 };
+    const present_lorie_feedback_target_rec *lorie_feedback_ptr = NULL;
@@ -115 +117,2 @@ proc_present_pixmap(ClientPtr client)
-    if (stuff->options & ~(PresentAllOptions)) {
+    if (stuff->options & ~(PresentAllOptions |
+                           LoriePresentOptionActualFeedback)) {
@@ -119,0 +123,15 @@ proc_present_pixmap(ClientPtr client)
+    if (stuff->options & LoriePresentOptionActualFeedback) {
+        RRCrtcPtr capability_crtc = target_crtc ? target_crtc :
+            present_get_crtc(window);
+
+        if (!(present_query_capabilities(capability_crtc) &
+              LoriePresentCapabilityActualFeedback)) {
+            client->errorValue = stuff->options;
+            return BadValue;
+        }
+        if (!present_lorie_get_feedback_target(window, client,
+                                               &lorie_feedback))
+            return BadMatch;
+        lorie_feedback_ptr = &lorie_feedback;
+    }
+
@@ -149 +167,2 @@ proc_present_pixmap(ClientPtr client)
-                         stuff->target_msc, stuff->divisor, stuff->remainder, notifies, nnotifies);
+                         stuff->target_msc, stuff->divisor, stuff->remainder,
+                         notifies, nnotifies, lorie_feedback_ptr);
diff --git a/present/present_scmd.c b/present/present_scmd.c
index 239055bc10d8c934555356c4213cda37dbff711e..b993e64e09c19852f700da63d557cc6fe18c8155 100644
--- a/present/present_scmd.c
+++ b/present/present_scmd.c
@@ -254,0 +255,15 @@ present_flip_try_ready(ScreenPtr screen)
+/* lorie: called when the renderer notifies that a GPU copy batch finished, or that it just lost
+ * its surface, instead of waiting for the next vblank poll. _safe iteration since
+ * present_re_execute() may destroy the vblank. */
+void
+lorieRecheckGpuCopies(void)
+{
+    present_vblank_ptr vblank, tmp;
+    Bool gpuCopyStalled = !lorieConnectionAlive() || !lorieRendererAvailable();
+
+    xorg_list_for_each_entry_safe(vblank, tmp, &present_exec_queue, event_queue) {
+        if (vblank->gpu_copy_pending && (lorieGpuCopyIsDone(vblank->gpu_copy_serial) || gpuCopyStalled))
+            present_re_execute(vblank);
+    }
+}
+
@@ -601,0 +617,12 @@ present_execute(present_vblank_ptr vblank, uint64_t ust, uint64_t crtc_msc)
+                {
+                    uint64_t lorie_present_tag = loriePreparePresentTag(
+                        vblank->window, (*screen->GetScreenPixmap)(screen));
+                    loriePublishPresentTag(lorie_present_tag,
+                                           vblank->window->drawable.id,
+                                           vblank->serial,
+                                           vblank->lorie_feedback.eid,
+                                           vblank->lorie_feedback.window_generation,
+                                           vblank->lorie_feedback.event_generation);
+                    if (lorie_present_tag)
+                        vblank->lorie_feedback_handed_off = TRUE;
+                }
@@ -682 +709,2 @@ present_scmd_pixmap(WindowPtr window,
-                    int num_notifies)
+                    int num_notifies,
+                    const present_lorie_feedback_target_rec *lorie_feedback)
@@ -763 +791,2 @@ present_scmd_pixmap(WindowPtr window,
-                                   crtc_msc);
+                                   crtc_msc,
+                                   lorie_feedback);
diff --git a/present/present_screen.c b/present/present_screen.c
index 2c29aafd23509143b491cc9c6209234faa026b33..42c57618667f0ae00fb161a19276f914fbe60651 100644
--- a/present/present_screen.c
+++ b/present/present_screen.c
@@ -27,0 +28 @@ DevPrivateKeyRec present_window_private_key;
+static uint64_t present_lorie_window_generation;
@@ -46,0 +48,3 @@ present_get_window_priv(WindowPtr window, Bool create)
+    window_priv->lorie_generation = ++present_lorie_window_generation;
+    if (!window_priv->lorie_generation)
+        window_priv->lorie_generation = ++present_lorie_window_generation;
diff --git a/present/present_vblank.c b/present/present_vblank.c
index a9f17d4b2be374257b44199959c5ae1beb2d3972..9d37a7e536d3ab472e3694e0566dfa7359802dca 100644
--- a/present/present_vblank.c
+++ b/present/present_vblank.c
@@ -24,0 +25,16 @@
+static void
+present_lorie_retire_feedback_unknown(present_vblank_ptr vblank)
+{
+    if (!vblank->lorie_feedback.eid ||
+        vblank->lorie_feedback_handed_off ||
+        vblank->lorie_feedback_terminal_sent)
+        return;
+
+    if (vblank->window)
+        present_lorie_send_actual_notify(vblank->window,
+                                         &vblank->lorie_feedback,
+                                         LoriePresentCompleteModeUnknown,
+                                         vblank->serial, 0, 0);
+    vblank->lorie_feedback_terminal_sent = TRUE;
+}
+
@@ -63 +79,2 @@ present_vblank_init(present_vblank_ptr vblank,
-                    uint64_t crtc_msc)
+                    uint64_t crtc_msc,
+                    const present_lorie_feedback_target_rec *lorie_feedback)
@@ -108,0 +126,2 @@ present_vblank_init(present_vblank_ptr vblank,
+    if (lorie_feedback)
+        vblank->lorie_feedback = *lorie_feedback;
@@ -166 +185,2 @@ present_vblank_create(WindowPtr window,
-                      uint64_t crtc_msc)
+                      uint64_t crtc_msc,
+                      const present_lorie_feedback_target_rec *lorie_feedback)
@@ -176 +196 @@ present_vblank_create(WindowPtr window,
-                            target_msc, crtc_msc))
+                            target_msc, crtc_msc, lorie_feedback))
@@ -190,0 +211,10 @@ present_vblank_scrap(present_vblank_ptr vblank)
+    present_lorie_retire_feedback_unknown(vblank);
+
+    /* lorie: an offloaded copy into vblank->pixmap may still be in flight - ack it now, while
+     * the pixmap is still valid, or the extra buffer references lorieTryScheduleGpuCopy took
+     * leak forever. */
+    if (vblank->gpu_copy_pending) {
+        lorieGpuCopyAck(vblank->pixmap, vblank->gpu_copy_dst_buffer);
+        vblank->gpu_copy_pending = FALSE;
+    }
+
@@ -202,0 +233,9 @@ present_vblank_destroy(present_vblank_ptr vblank)
+    present_lorie_retire_feedback_unknown(vblank);
+
+    /* lorie: same as in present_vblank_scrap() - must run before vblank->pixmap is dropped below,
+     * for the destroy paths that reach a still-pending vblank without going through scrap first. */
+    if (vblank->gpu_copy_pending) {
+        lorieGpuCopyAck(vblank->pixmap, vblank->gpu_copy_dst_buffer);
+        vblank->gpu_copy_pending = FALSE;
+    }
+
diff --git a/xkb/ddxLoad.c b/xkb/ddxLoad.c
index f9b7b06d901813d30fa4d0c2a39387039c805c6f..f4b2aeddcc380fb5f621dabc959cc326b524faec 100644
--- a/xkb/ddxLoad.c
+++ b/xkb/ddxLoad.c
@@ -58,0 +59,3 @@ THE USE OR PERFORMANCE OF THIS SOFTWARE.
+char* xkbcomp_argv[16] = {0};
+int xkbcomp_argc = 0;
+
@@ -154,0 +158,19 @@ RunXkbComp(xkbcomp_buffer_callback callback, void *userdata)
+    char buf2[256];
+    char buf3[256];
+    sprintf(buf2, "-R%s", XkbBaseDirectory);
+    sprintf(buf3, "%s%s.xkm", xkm_output_dir, keymap);
+    xkbcomp_argv[0] = "xkbcomp";
+    xkbcomp_argv[1] = "-w";
+    xkbcomp_argv[2] = "1";
+    xkbcomp_argv[3] = buf2;
+    xkbcomp_argv[4] = "-xkm";
+    xkbcomp_argv[5] = (char*) xkmfile;
+    xkbcomp_argv[6] = "-em1";
+    xkbcomp_argv[7] = PRE_ERROR_MSG;
+    xkbcomp_argv[8] = "-emp";
+    xkbcomp_argv[9] = ERROR_PREFIX;
+    xkbcomp_argv[10] = "-eml";
+    xkbcomp_argv[11] = POST_ERROR_MSG1;
+    xkbcomp_argv[12] = buf3;
+    xkbcomp_argc = 13;
+
